# Build output and generated files — never format these
dist/
coverage/
apps/web/src/routeTree.gen.ts
apps/web/src/paraglide/
# Written into the inlang project directory by the Paraglide compiler, so they
# only exist after a build — and `prettier --check .` runs after one in CI.
# `settings.json` beside them is hand-maintained and stays formatted.
apps/web/project.inlang/.meta.json
apps/web/project.inlang/README.md

# Drizzle migration snapshots are machine-generated by drizzle-kit;
# formatting them would make every future db:generate churn
packages/db/drizzle/

# Vendored Oxlint plugin (copied from the install-anti-slop skill); it is
# generator-owned and uses its own tab-based style
tools/oxlint/anti-slop/

# Lockfiles and package-manager artifacts
pnpm-lock.yaml

# Tooling artifacts
.claude/worktrees/
lighthouse-reports/
e2e/blob-report/
playwright/.cache/
.pi-lens/
